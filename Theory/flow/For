public class For {
    public static void main(String[] args){
//        for(int i = 0; i < 5; i++){ // syntax is for(declaration, condition, increment)
//            System.out.println(i);
//        }
//          0
//          1
//          2
//          3
//          4
        //for allows to loop through ode block if condition is fulfilled
        // its consist from 3 elements
        // first element is used as variable declaration and initialization int i = 0, its executed one time before code block
            // best practice is to start initialization at 0
            //it's called init
        // second is a condition that is checked each time i < 5
            //it's called condition
        // last element execute each time after code block was executed
            //it's called increment
            // important is to know, that it executes after code block
            // so if loop starts, first time it will execute based on conditions and then increment will work after


        for(int i = 1; i < 15; i++){
            System.out.println(i);
            if(i % 2 == 0) break;
        } // if we want to execute loop till one condition that is not written in loop parenthesis
        // we use break, what means break the loop and don't execute anymore

    }
}
